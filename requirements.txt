# Pinned versions — guaranteed to reproduce the published results.
# Generated from: pip install (Python 3.12, macOS 14, July 2026) then pip freeze.
# To install: pip install -r requirements.txt
#
# Dependency notes:
#   langchain-chroma 1.1.0 requires langchain-core>=1.1.3 — this rules out langchain 0.3.x.
#   langgraph 1.2.8 is the latest in the >=1.2.5,<1.3.0 range required by langchain-community 0.4.x.
#   The `langchain` (classic) package is pulled in transitively; no direct pin needed.

neo4j==6.2.0
langgraph==1.2.8
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langchain-community==0.4.2
langchain-core==1.4.8
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-ollama==1.1.0
fastembed==0.8.0
chromadb==1.5.9
pydantic==2.13.4
python-dotenv==1.2.2

# Dev / test
pytest==9.1.1
pytest-mock==3.15.1
