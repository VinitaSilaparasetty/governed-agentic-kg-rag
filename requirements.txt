neo4j>=5.19,<6
langgraph>=0.2,<1
langchain>=0.2,<1
langchain-community>=0.2,<1
langchain-text-splitters>=0.2,<1
fastembed>=0.3,<1          # ONNX-based embeddings; no PyTorch/GPU required
langchain-chroma>=0.1,<1
chromadb>=0.5,<1
langchain-ollama>=0.2,<1   # Free local LLM via Ollama (mistral, llama3, etc.)
pydantic>=2.7,<3
python-dotenv>=1.0,<2

# Optional: swap embedding provider via EMBEDDING_PROVIDER env var
# langchain-openai>=0.1      EMBEDDING_PROVIDER=openai

# Dev / test
pytest>=8.0
pytest-mock>=3.14
